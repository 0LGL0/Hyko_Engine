#pragma once
#include <stdint.h>

namespace Hyko {
	using keyCode = uint16_t;

	namespace Key {
		enum : keyCode {
			HK_KEYBOARD_NONE		   = -1,
			HK_KEYBOARD_SPACE		   = 32,	/* SPACE */
			HK_KEYBOARD_APOSTROPHE      = 39,	/* ` */
			HK_KEYBOARD_COMMA		   = 44,	/* , */
			HK_KEYBOARD_MINUS		   = 45,	/* - */
			HK_KEYBOARD_PERIOD		   = 46,	/* . */
			HK_KEYBOARD_SLASH		   = 47,	/* / */
			HK_KEYBOARD_0			   = 48,	/* 0 */
			HK_KEYBOARD_1			   = 49,	/* 1 */
			HK_KEYBOARD_2			   = 50,	/* 2 */
			HK_KEYBOARD_3			   = 51,	/* 3 */
			HK_KEYBOARD_4			   = 52,	/* 4 */
			HK_KEYBOARD_5			   = 53,	/* 5 */
			HK_KEYBOARD_6			   = 54,	/* 6 */
			HK_KEYBOARD_7			   = 55,	/* 7 */
			HK_KEYBOARD_8			   = 56,	/* 8 */
			HK_KEYBOARD_9			   = 57,	/* 9 */
			HK_KEYBOARD_SEMICOLON	   = 59,	/* ; */
			HK_KEYBOARD_EQUAL		   = 61,	/* = */
			HK_KEYBOARD_A			   = 65,	/* A */
			HK_KEYBOARD_B			   = 66,	/* B */
			HK_KEYBOARD_C			   = 67,	/* C */
			HK_KEYBOARD_D			   = 68,	/* D */
			HK_KEYBOARD_E			   = 69,	/* E */
			HK_KEYBOARD_F			   = 70,	/* F */
			HK_KEYBOARD_G			   = 71,	/* G */
			HK_KEYBOARD_H			   = 72,	/* H */
			HK_KEYBOARD_I			   = 73,	/* I */
			HK_KEYBOARD_J			   = 74,	/* J */
			HK_KEYBOARD_K			   = 75,	/* K */
			HK_KEYBOARD_L			   = 76,	/* L */
			HK_KEYBOARD_M			   = 77,	/* M */
			HK_KEYBOARD_N			   = 78,	/* N */
			HK_KEYBOARD_O			   = 79,	/* O */
			HK_KEYBOARD_P			   = 80,	/* P */
			HK_KEYBOARD_Q			   = 81,	/* Q */
			HK_KEYBOARD_R			   = 82,	/* R */
			HK_KEYBOARD_S			   = 83,	/* S */
			HK_KEYBOARD_T			   = 84,	/* T */
			HK_KEYBOARD_U			   = 85,	/* U */
			HK_KEYBOARD_V			   = 86,	/* V */
			HK_KEYBOARD_W			   = 87,	/* W */
			HK_KEYBOARD_X			   = 88,	/* X */
			HK_KEYBOARD_Y			   = 89,	/* Y */
			HK_KEYBOARD_Z			   = 90,	/* Z */
			HK_KEYBOARD_LEFT_BRACKET    = 91,	/* [ */
			HK_KEYBOARD_BACKSLASH	   = 92,	/* \*/
			HK_KEYBOARD_RIGHT_BRACKET   = 93,	/* ] */
			HK_KEYBOARD_GRAVE_ACCENT    = 96,	/* ' */
			HK_KEYBOARD_WORLD_1		   = 161,   /* non-US #1 */
			HK_KEYBOARD_WORLD_2		   = 162,   /* non-US #2 */
			HK_KEYBOARD_ESCAPE		   = 256,	/* ESCAPE */
			HK_KEYBOARD_ENTER		   = 257,	/* ENTER */
			HK_KEYBOARD_TAB			   = 258,	/* TAB */
			HK_KEYBOARD_BACKSPACE	   = 259,	/* BACKSPACE */
			HK_KEYBOARD_INSERT		   = 260,	/* INSERT */
			HK_KEYBOARD_DELETE		   = 261,	/* DELETE */
			HK_KEYBOARD_RIGHT		   = 262,	/* RIGHT ARROW */
			HK_KEYBOARD_LEFT			   = 263,	/* LEFT ARROW */
			HK_KEYBOARD_DOWN			   = 264,	/* DOWN ARROW */
			HK_KEYBOARD_UP			   = 265,	/* UP ARROW */
			HK_KEYBOARD_PAGE_UP		   = 266,	/* PAGE_UP */
			HK_KEYBOARD_PAGE_DOWN	   = 267,	/* PAGE_DOWN */
			HK_KEYBOARD_HOME			   = 268,	/* HOME */
			HK_KEYBOARD_END			   = 269,	/* END */
			HK_KEYBOARD_CAPS_LOCK	   = 280,	/* CAPS_LOCK */
			HK_KEYBOARD_SCROLL_LOCK     = 281,	/* SCROLL_LOCK */
			HK_KEYBOARD_NUM_LOCK        = 282,	/* NUM_LOCK */
			HK_KEYBOARD_PRINT_SCREEN    = 283,	/* PRINT_SCREEN */
			HK_KEYBOARD_PAUSE		   = 284,	/* PAUSE */
			HK_KEYBOARD_F1			   = 290,	/* F1 */
			HK_KEYBOARD_F2			   = 291,	/* F2 */
			HK_KEYBOARD_F3			   = 292,	/* F3 */
			HK_KEYBOARD_F4			   = 293,	/* F4 */
			HK_KEYBOARD_F5			   = 294,	/* F5 */
			HK_KEYBOARD_F6			   = 295,	/* F6 */
			HK_KEYBOARD_F7			   = 296,	/* F7 */
			HK_KEYBOARD_F8			   = 297,	/* F8 */
			HK_KEYBOARD_F9			   = 298,	/* F9 */
			HK_KEYBOARD_F10			   = 299,	/* F10 */
			HK_KEYBOARD_F11			   = 300,	/* F11 */
			HK_KEYBOARD_F12			   = 301,	/* F12 */
			HK_KEYBOARD_F13			   = 302,	/* F13 */
			HK_KEYBOARD_F14			   = 303,	/* F14 */
			HK_KEYBOARD_F15			   = 304,	/* F15 */
			HK_KEYBOARD_F16			   = 305,	/* F16 */
			HK_KEYBOARD_F17			   = 306,	/* F17 */
			HK_KEYBOARD_F18			   = 307,	/* F18 */
			HK_KEYBOARD_F19			   = 308,	/* F19 */
			HK_KEYBOARD_F20			   = 309,	/* F20 */
			HK_KEYBOARD_F21			   = 310,	/* F21 */
			HK_KEYBOARD_F22			   = 311,	/* F22 */
			HK_KEYBOARD_F23			   = 312,	/* F23 */
			HK_KEYBOARD_F24			   = 313,	/* F24 */
			HK_KEYBOARD_F25			   = 314,	/* F25 */
			HK_KEYBOARD_KEYPAD_0		   = 320,	/* KEYPAD 0 */
			HK_KEYBOARD_KEYPAD_1		   = 321,	/* KEYPAD 1 */
			HK_KEYBOARD_KEYPAD_2		   = 322,	/* KEYPAD 2 */
			HK_KEYBOARD_KEYPAD_3		   = 323,	/* KEYPAD 3 */
			HK_KEYBOARD_KEYPAD_4		   = 324,	/* KEYPAD 4 */
			HK_KEYBOARD_KEYPAD_5		   = 325,	/* KEYPAD 5 */
			HK_KEYBOARD_KEYPAD_6		   = 326,	/* KEYPAD 6 */
			HK_KEYBOARD_KEYPAD_7		   = 327,	/* KEYPAD 7 */
			HK_KEYBOARD_KEYPAD_8		   = 328,	/* KEYPAD 8 */
			HK_KEYBOARD_KEYPAD_9		   = 329,	/* KEYPAD 9 */
			HK_KEYBOARD_KEYPAD_DECIMAL  = 330,	/* DECIMAL */
			HK_KEYBOARD_KEYPAD_DIVIDE   = 331,	/* DIVIDE */
			HK_KEYBOARD_KEYPAD_MULTIPLY = 332,	/* MULTIPLY */
			HK_KEYBOARD_KEYPAD_SUBTRACT = 333,	/* SUBTRACT */
			HK_KEYBOARD_KEYPAD_ADD	   = 334,	/* ADD */
			HK_KEYBOARD_KEYPAD_ENTER    = 335,	/* ENTER */
			HK_KEYBOARD_KEYPAD_EQUAL    = 336,	/* EQUAL */
			HK_KEYBOARD_LEFT_SHIFT      = 340,	/* L_SHIFT */
			HK_KEYBOARD_LEFT_CONTROL    = 341,	/* L_CONTROL */
			HK_KEYBOARD_LEFT_ALT		   = 342,	/* L_ALT */
			HK_KEYBOARD_LEFT_SUPER	   = 343,	/* L_SUPER */
			HK_KEYBOARD_RIGHT_SHIFT	   = 344,	/* R_SHIFT */
			HK_KEYBOARD_RIGHT_CONTROL   = 345,	/* R_CONTROL */
			HK_KEYBOARD_RIGHT_ALT       = 346,	/* R_ALT */
			HK_KEYBOARD_RIGHT_SUPER	   = 347,	/* R_SUPER */
			HK_KEYBOARD_MENU			   = 348,	/* MENU */
			HK_KEYBOARD_LAST			   = 315,	/* LAST */
		};
	}
}